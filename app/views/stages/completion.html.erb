<%= form_with model: current_user, url: charge_user_path(current_user), method: :post, id: 'charge-form', local: true do |f| %>
<div class='charge_name'>
支払いが完了しました</br>
<%= current_user.nickname %>さん</br>
    残高 ¥.<%= current_user.money %>
    </div>
    <% end %>