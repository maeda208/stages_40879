<!DOCTYPE html>
<html>
  <head>
    <title>Stages40879</title>
    <meta name="csrf-param" content="authenticity_token" />
  <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  

    <%= stylesheet_link_tag 'application', media: 'all','data-turbolinks-track': 'reload'  %>
    <%= javascript_include_tag 'application', type: 'module', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
       <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">My Stage</a></h1>
      </div>
         <div class="user_nav_grid">
    <% if user_signed_in? %>
    <%= link_to current_user.nickname, user_path(current_user), class: :greeting__link %>
   <%= link_to "ログアウト", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: :nav__logout %>
    <%= link_to "新規募集", new_stage_path, class: :nav__btn %>
    <% else %>
    <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
    <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
    <% end %>
        </div>
    </header>
    <%= yield %>
     <footer>
      <p>
        <div class="copyright">
        Copyright My stage 2024.
        </div>
      </p>
    </footer>
  </body>
</html>
